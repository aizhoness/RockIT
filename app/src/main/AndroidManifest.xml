<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.aizhan.rockitapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.MANAGE_DOCUMENTS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label=""
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".SignupPart2Activity" />
        <activity
            android:name=".MusicianProfileActivity"
            android:label="@string/title_activity_musician_profile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ClientProfileActivity"
            android:label="@string/title_activity_client_profile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".FanProfileActivity"
            android:label="@string/title_activity_fan_profile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CreateOfferActivity" />
        <activity android:name=".SingleMusicianActivity" />
        <activity android:name=".SingleOfferActivity" />
        <activity android:name=".UploadVideoActivity"></activity>
    </application>

</manifest>